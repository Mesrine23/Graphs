#include "GraphTypes.h"
struct graph GRAPHinit(struct graph ,int);
struct graph GRAPHedge(struct graph, int, int, int*);
void showGRAPH(struct graph,int *);
void SimplePathCheck(struct graph, int *, int, int);

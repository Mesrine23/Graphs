#include "GraphTypes.h"
struct graph GRAPHinit(struct graph ,int);
struct graph GRAPHedge(struct graph, int, int, int, int*);
void showGRAPH(struct graph,int *);
void MinimumSpanningTree(struct graph ,int*);
